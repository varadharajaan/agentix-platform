---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: credentialproviders.mycelium.io
spec:
  group: mycelium.io
  names:
    categories:
    - mycelium
    kind: CredentialProvider
    listKind: CredentialProviderList
    plural: credentialproviders
    shortNames:
    - cp
    singular: credentialprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Whether the provider is ready
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          CredentialProvider is the Schema for the credentialproviders API.
          It represents either an OAuth 2.0 provider or an API key provider.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              CredentialProviderSpec defines the desired state of CredentialProvider.
              Exactly one of oauth or apiKey must be set.
            properties:
              apiKey:
                description: APIKey configures this as an API key credential provider.
                properties:
                  apiKeySecretRef:
                    description: |-
                      APIKeySecretRef references a key in a Kubernetes Secret containing the API key.
                      The key value is passed in the request body to the tool executor.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - apiKeySecretRef
                type: object
              oauth:
                description: OAuth configures this as an OAuth 2.0 credential provider.
                properties:
                  clientId:
                    description: ClientID is the OAuth client ID.
                    maxLength: 256
                    minLength: 1
                    type: string
                  clientSecretRef:
                    description: ClientSecretRef references a key in a Kubernetes
                      Secret containing the client secret.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  discovery:
                    description: Discovery contains the OAuth2 provider discovery
                      configuration.
                    properties:
                      authorizationServerMetadata:
                        description: AuthorizationServerMetadata provides explicit
                          OAuth2 server endpoints.
                        properties:
                          authorizationEndpoint:
                            description: AuthorizationEndpoint is the authorization
                              endpoint URL.
                            format: uri
                            maxLength: 2048
                            minLength: 1
                            type: string
                          issuer:
                            description: Issuer is the issuer URL for the OAuth2 authorization
                              server.
                            format: uri
                            maxLength: 2048
                            minLength: 1
                            type: string
                          responseTypes:
                            description: ResponseTypes are the supported response
                              types.
                            items:
                              type: string
                            maxItems: 8
                            type: array
                            x-kubernetes-validations:
                            - message: each response type must be 1-64 characters
                              rule: self.all(r, size(r) >= 1 && size(r) <= 64)
                          tokenEndpoint:
                            description: TokenEndpoint is the token endpoint URL.
                            format: uri
                            maxLength: 2048
                            minLength: 1
                            type: string
                          tokenEndpointAuthMethods:
                            description: TokenEndpointAuthMethods are the authentication
                              methods supported by the token endpoint.
                            items:
                              description: TokenEndpointAuthMethod specifies how clients
                                authenticate at the token endpoint.
                              enum:
                              - client_secret_post
                              - client_secret_basic
                              type: string
                            maxItems: 2
                            type: array
                        required:
                        - authorizationEndpoint
                        - issuer
                        - tokenEndpoint
                        type: object
                      discoveryUrl:
                        description: DiscoveryURL is the OIDC discovery endpoint.
                        format: uri
                        maxLength: 2048
                        pattern: .+/\.well-known/openid-configuration
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of the fields in [discoveryUrl authorizationServerMetadata]
                        must be set
                      rule: '[has(self.discoveryUrl),has(self.authorizationServerMetadata)].filter(x,x==true).size()
                        == 1'
                required:
                - clientId
                - clientSecretRef
                - discovery
                type: object
            type: object
            x-kubernetes-validations:
            - message: exactly one of the fields in [oauth apiKey] must be set
              rule: '[has(self.oauth),has(self.apiKey)].filter(x,x==true).size() ==
                1'
          status:
            description: CredentialProviderStatus defines the observed state of CredentialProvider.
            properties:
              conditions:
                description: |-
                  Conditions represent the latest observations.
                  Known condition types: "Ready"
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
