apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  namespace: tenant-alpha
spec:
  containers:
    - name: nginx
      image: nginx:latest
  nodeSelector:
    node-role: agents
  tolerations:
    - key: workload
      operator: Equal
      value: agents
      effect: NoSchedule
