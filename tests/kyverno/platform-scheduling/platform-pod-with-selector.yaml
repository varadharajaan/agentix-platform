apiVersion: v1
kind: Pod
metadata:
  name: custom-scheduled-pod
  namespace: kagent-system
spec:
  containers:
    - name: controller
      image: ghcr.io/kagent-dev/controller:latest
  nodeSelector:
    node-role: gateway
